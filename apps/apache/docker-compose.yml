# image: https://hub.docker.com/_/httpd

version: '3.8'
services:
  apache:
    container_name: ${W9_NAME}
    image: httpd:${W9_VERSION}
    restart: unless-stopped
    ports:
     - ${W9_HTTP_PORT}:80
    volumes:
     - apache:/usr/local/apache2/htdocs
     
networks:
  default:
    name: ${W9_NETWORK}
    external: true
    
volumes:
  apache:
    driver: local
