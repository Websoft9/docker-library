version: '3'
services:
  web:
    image: $W9_REPO:$W9_VERSION
    container_name: $W9_ID
    ports:
      - "$W9_HTTP_PORT_SET:8787"
    restart: always
    
networks:
  default:
    name: $W9_NETWORK
    external: true

