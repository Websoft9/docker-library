# Appwrite

## FAQ

- On first access, open `/console` and create the initial administrator account.
- After version upgrades that require data migration, run `docker compose exec appwrite migrate` inside the deployed app directory after backup.
