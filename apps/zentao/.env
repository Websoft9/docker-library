APP_REPO=easysoft/zentao
APP_DIST=community
APP_VERSION=18.8

APP_ID=zentao

#### --  Not allowed to edit below environments when recreate app based on existing data  -- ####

POWER_PASSWORD=uQ8E1wVTzG8SOk3!

# Environments which for user settings when create application
APP_HTTP_PORT_SET=9001

APP_HTTP_PORT=80
APP_URL=Internet_IP:$APP_HTTP_PORT_SET

APP_DB_EXPOSE="mysql"

#### ----------------------------------------------------------------------------------------- ####

APP_NETWORK=websoft9
APP_NAME=zentao

##--------------- Zentao environments for user --------------------------------------------------##
ZT_MYSQL_HOST=$APP_ID-mysql
ZT_MYSQL_PORT=3306
ZT_MYSQL_USER=$APP_ID
ZT_MYSQL_PASSWORD=$POWER_PASSWORD
ZT_MYSQL_DB=$APP_ID