# Generated by teleport-init from teleport.yaml.tmpl.
# Keep this file present because docker-compose.yml mounts ./src/config into the
# container, but do not edit it directly. Any changes will be overwritten by the
# init service before Teleport starts.
