# Teleport serves its Web UI and API over HTTPS with a self-signed certificate,
# so the adaptive HTTP web-access check does not apply. The custom script waits
# for the web API over HTTPS instead.
skip:
  - id: web-access

optional:
  - id: webapi-https
    type: script
    script: check.sh
