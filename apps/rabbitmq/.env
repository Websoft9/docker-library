W9_REPO=rabbitmq
W9_DIST=community
W9_VERSION=4.3-management
W9_POWER_PASSWORD="!3d7qZRrN1nJ9nzK"

# Canonical semantics live in docs/w9-env-spec.md.
# This template controls layout; the spec controls meaning and decision rules.

#### --  Not allowed to edit below environments when recreate app based on existing data  -- ####

W9_ID=rabbitmq
W9_HTTP_PORT=15672
W9_HTTP_PORT_SET=15672
W9_MQ_PORT_SET=5672
W9_ER_PORT_SET=4369
W9_LOGIN_USER=admin
W9_LOGIN_PASSWORD=${W9_POWER_PASSWORD}
W9_URL=appname.example.com
W9_NETWORK=websoft9

#### ----------------------------------------------------------------------------------------- ####

# ============================================================
# RabbitMQ image environment variables
# Docs: https://hub.docker.com/_/rabbitmq
# Follow docs/w9-env-spec.md when deciding which vars belong here.
# Enable the vars actually used in docker-compose.yml below; list up
# to 5 unused vars commented out. Full reference lives in the Docs URL.
# ============================================================

# Used by docker-compose.yml:
RABBITMQ_DEFAULT_USER=${W9_LOGIN_USER}
RABBITMQ_DEFAULT_PASS=${W9_LOGIN_PASSWORD}

# Not used by default; enable only when needed:
# RABBITMQ_DEFAULT_VHOST=/
# RABBITMQ_NODENAME=rabbit@localhost
# RABBITMQ_ERLANG_COOKIE=
# RABBITMQ_VM_MEMORY_HIGH_WATERMARK=0.6
# RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=
