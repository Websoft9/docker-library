# Umbraco serves the backoffice over HTTPS with a self-signed certificate, so
# the adaptive HTTP web-access check does not apply.
skip:
  - id: web-access

optional:
  - id: backoffice-https
    type: script
    script: check.sh
