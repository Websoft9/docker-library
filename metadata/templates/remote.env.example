# Remote execution profile for remote-capable commands and skills.
# Copy to .secrets/remote.env and edit values locally (git-ignored).

TARGET=remote
SSH_HOST=127.0.0.1
SSH_USER=root
SSH_SECRET_PATH=.secrets/ssh/default.pem
DEPLOY_ROOT=/opt/websoft9-test/apps
# CONTAINER=websoft9
