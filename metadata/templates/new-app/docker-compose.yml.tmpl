services:

  {{ name }}:
    image: $W9_REPO:$W9_VERSION
    container_name: $W9_ID
    restart: unless-stopped
    env_file: .env
    # TODO: add ports, volumes, healthcheck, and dependency services after upstream research

networks:
  default:
    name: $W9_NETWORK
    external: true
