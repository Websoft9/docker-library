W9_REPO={repo}
W9_DIST=community
W9_VERSION={version}

#### --  Not allowed to edit below environments when recreate app based on existing data  -- ####

W9_ID={name}

W9_NETWORK=websoft9

#### ----------------------------------------------------------------------------------------- ####

# --- password group: enable when the app has a DB or login (upstream research decides) ---
# W9_POWER_PASSWORD="{power_password}"
# W9_LOGIN_USER=admin
# W9_LOGIN_PASSWORD=$W9_POWER_PASSWORD

# --- web group: enable when the app exposes a web page ---
# W9_HTTP_PORT_SET=9001
# W9_HTTPS_PORT_SET=9002
# W9_ADMIN_PATH=""

#W9_NAME=""
#W9_RCODE=""
