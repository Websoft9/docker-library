# image: https://hub.docker.com/r/bitnami/suitecrm
# docs: https://github.com/bitnami/bitnami-docker-suitecrm


version: '3.8'

services:
  suitecrm:
    image: docker.io/bitnami/suitecrm:${W9_VERSION}
    container_name: ${W9_NAME}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${W9_HTTP_PORT}:8080'
      - '${W9_HTTPS_PORT}:8443'
    environment:
      - SUITECRM_DATABASE_HOST=mysql
      - SUITECRM_DATABASE_PORT_NUMBER=${W9_DB_MYSQL_PORT}
      - SUITECRM_DATABASE_USER=${W9_DB_MYSQL_USER}
      - SUITECRM_DATABASE_NAME=${W9_DB_MYSQL_NAME}
      - SUITECRM_DATABASE_PASSWORD=${W9_DB_MYSQL_PASSWORD}
      - SUITECRM_USERNAME=${W9_USER}
      - SUITECRM_PASSWORD=${W9_PASSWORD}
      - SUITECRM_EMAIL=${W9_EMAIL}
      - SUITECRM_SKIP_BOOTSTRAP=no
      - ALLOW_EMPTY_PASSWORD=no
    volumes:
      - 'suitecrm:/bitnami/suitecrm'
    depends_on:
      - mysql
    labels:
      com.docker.compose.w9_http.port: 8080
      com.docker.compose.w9_https.port: 8443
      com.docker.compose.w9_ssh.port: 22
    

  mysql:
    image: mysql:${W9_DB_MYSQL_VERSION}
    container_name: ${W9_NAME}-mysql
    command: --default-authentication-plugin=mysql_native_password    
    environment:
      MYSQL_ROOT_PASSWORD: ${W9_DB_MYSQL_PASSWORD}
      MYSQL_DATABASE: ${W9_DB_MYSQL_NAME}
      MYSQL_USER: ${W9_DB_MYSQL_USER}
      MYSQL_PASSWORD: ${W9_DB_MYSQL_PASSWORD}
    volumes:
      - mysql:/var/lib/mysql
    restart: always
    labels:
      com.docker.compose.websoft9.db.connection: true

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:${W9_DB_ES_VERSION}
    container_name: ${W9_NAME}-elasticsearch
    restart: unless-stopped
    ports:
        - ${W9_DB_ES_HTTP_PORT}:9200
        - ${W9_DB_ES_TCP_PORT}:9300
    environment:
        - discovery.type=single-node
        - xpack.security.enabled=false
        - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
networks:
  default:
    name: ${W9_NETWORK}
    external: true

volumes:
  mysql:
  suitecrm:
